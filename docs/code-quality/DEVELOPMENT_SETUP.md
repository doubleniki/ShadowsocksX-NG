# Development Setup Guide

## ????????? ????????? ??????????

### ????????? ????????????

1. **SwiftLint** - ?????? ??? Swift ????
   ```bash
   brew install swiftlint
   ```

2. **CocoaPods** - ???????? ????????????
   ```bash
   sudo gem install cocoapods
   ```

3. **????????? Git hooks**
   ```bash
   ./scripts/setup-git-hooks.sh
   ```

### ???????? ????

????? ???????? ????????????? ??????????? SwiftLint. ????? ????????? ??? ???????:

```bash
# ????????? ???? ??????
swiftlint

# ????????? ?????????? ????
swiftlint lint --path ShadowsocksX-NG/AppDelegate.swift

# ????????????? ????????? ????????? ????????
swiftlint autocorrect
```

### ????????? Xcode

1. **Warnings as Errors**: Build Settings ? "Treat Warnings as Errors" ? Yes (??? Release)
2. **SwiftLint Build Phase**:
   - Target ? Build Phases ? + ? New Run Script Phase
   - ??????:
     ```bash
     if which swiftlint >/dev/null; then
       swiftlint
     else
       echo "warning: SwiftLint not installed"
     fi
     ```

### ????????? ???????????

??. [SWIFT_STYLE_GUIDE.md](./SWIFT_STYLE_GUIDE.md) ??? ??????? ??????????? ?? ????? ????.

### ????????? ???????

? **?????????:**
- Force unwrapping (`!`) - ????? IBOutlets
- Force try (`try!`)
- Force cast (`as!`)
- Empty catch blocks
- `NSLog` (??????????? `os.log`)
- `print()` ? production ????

? **?????????????:**
- Guard statements ??? early returns
- Codable ??? ????????????
- async/await ??? ???????????? ????
- Protocol-based dependency injection
- Comprehensive error handling

### ?????? ??????

```bash
# ??? ?????
xcodebuild test \
  -workspace ShadowsocksX-NG.xcworkspace \
  -scheme ShadowsocksX-NG \
  -destination 'platform=macOS'

# ? ????????? ????
xcodebuild test \
  -workspace ShadowsocksX-NG.xcworkspace \
  -scheme ShadowsocksX-NG \
  -destination 'platform=macOS' \
  -enableCodeCoverage YES
```

### Workflow

1. ???????? feature branch ?? `develop`
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/your-feature-name
   ```

2. ??????? ????????? ? ?????????
   ```bash
   git add .
   git commit -m "feat: your feature description"
   ```

3. ???????? ? ???????? Pull Request
   ```bash
   git push origin feature/your-feature-name
   ```

### CI/CD

GitHub Actions ????????????? ?????????:
- SwiftLint ???????
- ?????? ???????
- ?????? ??????
- ???????? ????

??. `.github/workflows/code-quality.yml`
